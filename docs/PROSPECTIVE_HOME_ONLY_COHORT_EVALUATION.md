# Prospective home-only matched-budget cohort evaluation

Activation is fixed at `2026-07-19T15:00:00Z`. Only original-stream candidate rows whose raw selected outcome is home are eligible. Existing volume, uncertainty, cutoff and concentration gates remain unchanged. The result is written separately and does not alter the three existing campaign evaluations.
