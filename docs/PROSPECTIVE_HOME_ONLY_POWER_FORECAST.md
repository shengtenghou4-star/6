# Prospective home-only evidence-volume forecast

This outcome-blind forecast applies the existing cycle-based evidence model to original-stream candidates whose raw selected outcome is home after `2026-07-19T15:00:00Z`.

The final evaluation rules remain unchanged. Forecast output is operational planning evidence only and may not alter activation, quotas, sample requirements, uncertainty checks, or concentration limits.

Runs are stored immutably on `prospective-data` with a latest operational pointer.
