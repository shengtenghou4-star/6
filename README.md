# Prospective data branch

This branch stores immutable authenticated odds snapshots, run manifests, audit evidence and the latest reproducible derived state for the bounded prospective campaign.

It intentionally contains no GitHub Actions workflows. Collection code and research policies live on `main`.
